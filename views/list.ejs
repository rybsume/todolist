<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, height=device-height initial-scale=1.0">
    <title>To Do List</title>
</head>

<body>
    <% if(dayName === "Saturday" || dayName === "Sunday"){%>
    <h1 style="color: green;">It's <%= dayName %>.</h1>
    <% }else{%>
    <h1 style="color: blueviolet;">It's <%= dayName %>.</h1>
   <%} %>
</body>

</html>